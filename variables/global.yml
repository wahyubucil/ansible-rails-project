# Override the defaults variable

ansible_python_interpreter: /usr/bin/python3

ruby_version: ruby-2.6.3

# Install path for rvm (defaults to system wide)
ruby_rvm_install_path: /usr/share/rvm

# Default user & database to ensure exist.
postgresql_default_username: ubuntu
postgresql_default_password: secretpassword
