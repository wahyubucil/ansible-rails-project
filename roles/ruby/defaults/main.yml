---
# defaults file for ruby

ruby_version: ruby-2.6.3

# Install path for rvm (defaults to system wide)
ruby_rvm_install_path: "/home/{{ ansible_user }}/.rvm"

ruby_rails_default_gems:
  - "bundler"
  - "rails:6.0.1"
